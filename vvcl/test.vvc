User: <
    name: String,
    password: String,
    >;

Admin: <
    user: User,
    level: Int,
    >;

SomeEnum: | Lol` Int | Xd`;

user = (name: String, password: String) -> User {
    <name=name; password=password;>
};

triple = (a: Int) -> Int {
    double((a*3))
};

takes_string = (a: String) -> String {
    a
};

add = (a: Int, b: Int) -> Int (a+b);

u = user("a");

main = (a: Bool, b: Int) -> List<Int> {
    list_map([1,2,b], add(10))
};
