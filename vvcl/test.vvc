User: <
    name: String,
    password: String,
    >;

Admin: <
    user: User,
    level: Int,
    >;

SomeEnum: | Lol` Int | Xd`;

user = (name: String, password: String) -> User {
    <name=name; password=password;>
};

triple = (a: Int) -> Int {
    double((a*3))
};

takes_string = (a: String) -> String {
    a
};

add = (a: Int, b: Int) -> Int (a+b);

u = user("a");

main = (a: Bool, b: Int) -> User {
    thirty_two = add(30,2);
    added = add(10,b);
    tripled = triple(added);
    str = int_to_str(tripled);
    u(str)
};
