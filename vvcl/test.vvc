User: <
    name: String,
    password: String,
    >;

Admin: <
    user: User,
    level: Int,
    >;

user = (name: String, password: String) -> User {
    <name=name; password=password;>
};

main = (a: Bool, b: Int) -> Admin {
    u = user("a", "b");
    < user = u; level = b;>
};
