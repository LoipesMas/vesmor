User = $<
    name: String,
    password: String,
    >;

Admin = $<
    user: User,
    level: Int,
    >;

rec = < level = 5;>;

main = (a: Bool, b: User) -> Admin {
    new_level = 7;
    < rec | user = b; level = new_level;>
};
